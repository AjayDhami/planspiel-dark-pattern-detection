import React from 'react';
import { AdminExperts, AdminWebsiteDetails } from '../../types';
import { DialogTitle, FormControl, InputLabel, Select, MenuItem, DialogContent, FormGroup, FormControlLabel, Switch, DialogActions, Button } from '@mui/material';
import { Link } from 'react-router-dom';

const AssignExpert: React.FC<AdminWebsiteDetails> = ({websiteId, baseUrl, websiteName}) => {



  return (
    <>
       <DialogTitle>Heyyyy</DialogTitle>
    </>
  );
}; 

export default AssignExpert